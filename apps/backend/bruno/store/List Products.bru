meta {
  name: List Products
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/store/products
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.data: isArray
}

docs {
  Lista todos os produtos ativos do catálogo público na vitrine da loja (Storefront).
}
