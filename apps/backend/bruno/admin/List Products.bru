meta {
  name: List Products
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/admin/products
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.data: isArray
}

docs {
  Lista todos os produtos cadastrados para o painel administrativo do Nexus Commerce.
}
